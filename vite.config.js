/* import { defineConfig } from "vite";

export default defineConfig({
    base: "/Portfolio/",
    build: {
        manifest: true,
        rollupOptions: {
            input: "/main.js"
        }
    }
})
 */

export default {
    base: "/Portfolio/",
}

